.isaac/

# macOS Finder metadata
.DS_Store
**/.DS_Store

# Internal working notes: publication runbook, submission package, status,
# demo script, and specs. Kept local only — excluded from the public repo
# (contains internal process + live workspace identifiers). Not for publication.
human-input/

# clasp project IDs are specific to the original Apps Script project.
# Forkers should run `clasp create`/`clasp clone` to generate their own.
.clasp.json
